<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        table {
            width: 400px;
            /* border-collapse: collapse; */
        }
        table, th, td {
            border: 1px solid black;
            padding: 8px;
            text-align: left;
        }
        .empty {
            border: none;
        }
    </style>
</head>
<body>
    <% if(ques.length!=0){ %>
        <% for (let i = 0; i < ques.length; i++) { %>
            marks: <%= ques[i].marksOfEachQues %>
            <table>
                <tr>
                    <th>Question</th>
                    <th>Options</th>
                </tr>
                <% for(let j=0; j < (ques[i].options).length; j++) { %>
                    <tr>
                        <td class="empty">
                            <% if(j == (((ques[i].options).length)-1)) { %>
                                <form action="/teacherLogin/<%=uploadID%>/uploads/viewAllUploads/delete/<%=ques[i]._id%>", method="post">
                                    <button>delete</button>
                                </form>
                            <% } %>
                            
                            <% if(j == 0) { %>
                                <%= ques[i].question %>
                            <% } %>
                        </td>
                        <td><%= ques[i].options[j] %></td>
                    </tr>
                <% } %>
            </table> <br>
            Total marks: <%= ques[i].totalMarks %> <br><br>
            correct answers: 
            <% for(let k=0; k<(ques[i].correctAns).length; k++) { %>
                <p><%= ques[i].correctAns[k] %></p>
            <% } %>
        <% } %>
    <% } else{ %>
        No questions added!
    <% } %>
    <br><br>
    <form action="/teacherLogin/<%=uploadID%>/uploads", method="get">
        <button>back</button>
    </form>

</body>
</html>