<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        table {
            border-collapse: collapse;
            width: 50%;
            margin: 20px;
        }
        th, td {
            border: 1px solid black;
            padding: 10px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <h1>Welcome <%=username%></h1>
    <form id="uploadsForm" method="get"> 
        <button style="float: right;">upload questions for a quiz</button>
    </form>

    <p><%=subject.code%>: &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<%=subject.name%> </p>
    <table>
        <thead>
            <tr>
                <th>Email</th>
                <th>Username</th>
            </tr>
        </thead>
        <tbody>
            <% for (subStud of subStudArr) { %>
                <tr>
                    <td><%= subStud.email%></td>
                    <td><%= subStud.username %></td>
                </tr>
            <% } %>
        </tbody>
    </table>
    

    <script>
        const teacher_Id = '<%= id %>';
        document.getElementById("uploadsForm").action = `/teacherLogin/${teacher_Id}/uploads`;
    </script>

</body>
</html>